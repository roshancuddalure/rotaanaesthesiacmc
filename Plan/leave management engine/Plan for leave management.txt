1.the purpose of this is to know who are all applied for which leave slots on which date.
2. the leaves should be properly mapped to the department member profile so that it can be used for the workflow in the rota slots generator engine.
3. also the leave slot will be uploaded in the excel sheet or csv sheet whichever is easy for data extraction and analysis.
4. the workflow for leave management engine in our software is:
   ->the rota board member will upload the leave for a upcoming month in excel or csv( to be confirmed by u which format is recommended )
    ->after the upload we need to parse the data and classify the leaves slots into a internal calender which can later be altered by the rota board member.( the number of slot at each level and the leave slots levels i will tell).once the data is organised and structured from the uploaded file to be added to the db of the website in an structured form for the rota team and rota board member to see the leave on each day across that month and upcoming or previous month.
    ->the leaves should be then mapped to the concerned person and the unit to which that person is assigned in that specific/corresponding month and unit wise members leave statistics to be presented with an overview dashboard with toggle between months for the rota team to be having a idea.
       if any change in the leave slot is needed prior to the start of that month then  need an edit option on the slot and when entering leave the slot should not have free text rather should be able to select from the list populated from the finalised department member list( that shoudl be linked to avoid confusion in spellings of people and for accurate data in future).
